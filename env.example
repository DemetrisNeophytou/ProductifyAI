# =============================================================================
# ProductifyAI Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# For production, set these in Vercel (frontend) and Render (backend)
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Core Application
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=5050

# -----------------------------------------------------------------------------
# REQUIRED - Database
# -----------------------------------------------------------------------------
# PostgreSQL connection string (Supabase or self-hosted)
DATABASE_URL=postgresql://user:password@localhost:5432/productifyai

# Alternative: Supabase connection (if using Supabase)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------------------------------------------
# REQUIRED - Authentication & Security
# -----------------------------------------------------------------------------
# Replit Auth (OpenID Connect)
ISSUER_URL=https://replit.com/oidc
REPLIT_DOMAINS=localhost:5173,your-domain.com
REPL_ID=your-repl-id

# Google OAuth (Optional - for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# Session & JWT secrets (min 32 characters each)
JWT_SECRET=your-jwt-secret-min-32-characters-long-random-string
SESSION_SECRET=your-session-secret-min-32-characters-long-random-string

# -----------------------------------------------------------------------------
# REQUIRED - AI Services
# -----------------------------------------------------------------------------
# OpenAI API key for GPT-5 and DALL-E
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-5

# -----------------------------------------------------------------------------
# OPTIONAL - Stripe Payments (will use mock mode if not set)
# -----------------------------------------------------------------------------
# Stripe secret key (from Stripe Dashboard)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe webhook secret (from Stripe Dashboard → Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Price IDs (from Stripe Dashboard → Products)
STRIPE_PRICE_ID_PLUS=price_xxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Credits Price IDs (optional)
STRIPE_CREDITS_100_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_CREDITS_500_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_CREDITS_1000_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxxxx

# Force mock mode (set to 'true' to disable Stripe even if keys are set)
MOCK_STRIPE=false

# -----------------------------------------------------------------------------
# OPTIONAL - Email (Resend)
# -----------------------------------------------------------------------------
# Resend API key (from https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=ProductifyAI <noreply@productifyai.com>

# -----------------------------------------------------------------------------
# OPTIONAL - Media APIs
# -----------------------------------------------------------------------------
# Pexels API key (for stock photos/videos)
PEXELS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Pixabay API key (alternative stock photos)
PIXABAY_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Fonts API key (optional, has fallback)
GOOGLE_FONTS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# OPTIONAL - Frontend Environment Variables (Vite)
# -----------------------------------------------------------------------------
# API base URL (backend endpoint)
VITE_API_URL=http://localhost:5050

# Supabase frontend keys (if using Supabase)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe public key (for frontend Stripe.js)
VITE_STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Feature flags
VITE_SHOW_DEV_BANNER=true
VITE_MOCK_DB=false
VITE_MOCK_STRIPE=false
VITE_EVAL_MODE=false

# -----------------------------------------------------------------------------
# OPTIONAL - Development & Debugging
# -----------------------------------------------------------------------------
# Use mock database (in-memory, no Docker required)
MOCK_DB=false

# Replit development domain (for local testing)
REPLIT_DEV_DOMAIN=your-repl-name.repl.co

# CORS allowed origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,https://productivity-ai-gamma.vercel.app

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# 1. For local development, minimum required:
#    - DATABASE_URL (or MOCK_DB=true)
#    - OPENAI_API_KEY
#    - JWT_SECRET (min 32 chars)
#    - SESSION_SECRET (min 32 chars)
#    - REPLIT_DOMAINS
#    - REPL_ID
#
# 2. Stripe, Resend, and media APIs are optional:
#    - Payments will be mocked if Stripe keys are missing
#    - Emails will be logged to console if Resend key is missing
#    - Media APIs have fallbacks or will return errors
#
# 3. Frontend variables (VITE_*) are only used in client code
#    - Set these in Vercel for production frontend
#    - Backend doesn't need VITE_* variables
#
# 4. Production validation:
#    - NODE_ENV=production enables strict validation
#    - Missing required vars will cause server to exit
#    - Optional vars will log warnings but allow startup
